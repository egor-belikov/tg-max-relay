# Тот же токен, что у kinodolgoletiebot (совпадает с kinodolgoletie/.env).
API_TOKEN=

# Тот же секрет, что в kinodolgoletie/.env (INTERNAL_TELEGRAM_POLLER_SECRET).
INTERNAL_TELEGRAM_POLLER_SECRET=

# Куда слать Update JSON (одна строка на апдейт).
TELEGRAM_UPDATE_FORWARD_URL=http://kinodolgoletie:8080/internal/telegram-update

# Опционально: зеркало api.telegram.org
# TELEGRAM_API_URL=https://api.telegram.org
